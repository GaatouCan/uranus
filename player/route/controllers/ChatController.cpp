#include "ChatController.h"

namespace protocol {
    ChatController::ChatController() {
    }

    ChatController::~ChatController() {
    }

    void ChatController::OnRequest(GamePlayer *plr, const Message &msg) {
    }
}
